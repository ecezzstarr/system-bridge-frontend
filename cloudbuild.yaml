# Cloud Build configuration for automated deployment
# This file orchestrates the build and deployment process to Cloud Run

steps:
  # Step 1: Verify the checked-out source builds before any image is pushed.
  - name: 'node:22'
    id: 'verify-source-build'
    entrypoint: 'bash'
    args:
      - '-lc'
      - 'npm ci --legacy-peer-deps && ./scripts/verify-build.sh'

  # Step 2: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:${_COMMIT_SHA}'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'
      - '--build-arg'
      - 'COMMIT_SHA=${_COMMIT_SHA}'
      - '--build-arg'
      - 'BUILD_TIMESTAMP=${_BUILD_TIMESTAMP}'
      - '.'
    env:
      - 'DOCKER_BUILDKIT=1'
    waitFor: ['verify-source-build']

  # Step 3: Push image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-image'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:${_COMMIT_SHA}'
    waitFor: ['build-image']

  # Step 4: Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-cloud-run'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image'
      - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:${_COMMIT_SHA}'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'COMMIT_SHA=${_COMMIT_SHA},BUILD_TIMESTAMP=${_BUILD_TIMESTAMP},NODE_ENV=production'
      - '--set-secrets'
      - 'DATABASE_URL=database-url:latest,NEXTAUTH_SECRET=nextauth-secret:latest,GOOGLE_AI_KEY=google-ai-key:latest,EIGHT_INTERNAL_TOKEN=eight-internal-token:latest,FLW_SECRET_KEY=flw-secret-key:latest'
    waitFor: ['push-image']

# Substitutions for environment-specific values
substitutions:
  _SERVICE_NAME: 'system-bridge-frontend'
  _REGION: 'us-central1'
  _COMMIT_SHA: '${COMMIT_SHA}'
  _BUILD_TIMESTAMP: '${BUILD_ID}'

# Image output
images:
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:${_COMMIT_SHA}'
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'

# Timeout for the entire build process
timeout: 3600s

# Options for the build
options:
  machineType: 'N1_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY