'use client'

import { getClientToken } from '@/lib/client-auth'
import { useState } from 'react'
import { ArrowDownLeft, ArrowUpRight, Bot, Boxes, BriefcaseBusiness, ChevronRight, CircleDollarSign, Copy, Cpu, ExternalLink, GitBranch, MessageSquare, ShieldCheck, Store, Users, Wallet, RefreshCw, Globe2 } from 'lucide-react'

interface WorkshopProps {
  client: { id?: string; name: string; file_number: string | null; business_name: string }
  folder: { workshop_type?: string; status?: string } | null
  vault: { balance: number; currency: string; withdrawals?: { id:string; amount:number; currency:string; destination:string; status:string }[]; siblings_funds?: { flameCoin:number; trx:number; usdt:number }; main_wallet?: { flameCoin:number; usdt:number } }
  bridge: { name: string; username?: string; email?: string; whatsapp_number?: string } | null
  approvedAgents: { id: string; name: string; username?: string; email?: string }[]
  workshop: { title: string; purpose: string; modules: string[]; type: string }
  bridgeAi: { name: string; purpose: string; last_report?: { summary: string } | null }
  businessStore?: { public_slug:string; public_url:string; name:string; description?:string; items:any[]; orders:any[] } | null
  internationalPayments?: { enabled:boolean; provider:string; account_name?:string|null; receiving_identifier?:string|null; payment_link?:string|null; supported_currencies:string; service_fee_percent:number; instructions?:string|null } | null
}

const icons: Record<string, any> = { Market:CircleDollarSign,Buy:ArrowDownLeft,Sell:ArrowUpRight,Holdings:Wallet,Orders:GitBranch,Activity:MessageSquare,'Business Store':Store,'International Payments':Globe2,'Business Formation':BriefcaseBusiness,Technology:Cpu,Interaction:MessageSquare,Formation:Boxes,Business:BriefcaseBusiness,Productivity:Cpu }

export default function ClientWorkshopWorld(props: WorkshopProps) {
  const [active,setActive]=useState(props.workshop.modules[0] || 'Business Store')
  const [reporting,setReporting]=useState(false)
  const [reportMessage,setReportMessage]=useState('')
  const initialPayments=props.internationalPayments || { enabled:false, provider:'wise', account_name:'', receiving_identifier:'', payment_link:'', supported_currencies:'USD,EUR,GBP', service_fee_percent:15, instructions:'' }
  const [payments,setPayments]=useState<any>(initialPayments)
  const [savingPayments,setSavingPayments]=useState(false)
  const [paymentMessage,setPaymentMessage]=useState('')
  const crypto=props.workshop.type==='crypto_exchange'
  const modules=props.workshop.modules
  const report=async()=>{setReporting(true);setReportMessage('');try{const token=getClientToken();const res=await fetch('/api/bridge-ai/report',{method:'POST',headers:{Authorization:`Bearer ${token}`}});const body=await res.json();if(!res.ok)throw new Error(body.error||'Report failed');setReportMessage('Bridge AI has reported the interaction insight to Administration.')}catch(e:any){setReportMessage(e.message||'Bridge AI report failed')}finally{setReporting(false)}}
  const savePayments=async()=>{setSavingPayments(true);setPaymentMessage('');try{const token=getClientToken();const res=await fetch('/api/client/international-payments',{method:'PUT',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(payments)});const body=await res.json();if(!res.ok)throw new Error(body.error||'Unable to save payment settings');setPayments(body.profile);setPaymentMessage('International payment door updated.')}catch(e:any){setPaymentMessage(e.message||'Unable to save payment settings')}finally{setSavingPayments(false)}}
  const publicUrl=props.businessStore?.public_url || null
  return <section className="rounded-[2rem] border border-white/10 bg-slate-950/95 overflow-hidden shadow-2xl">
    <div className="border-b border-white/10 bg-gradient-to-r from-slate-950 via-slate-900 to-black p-6 md:p-8"><div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-[10px] uppercase tracking-[0.35em] text-sky-300">System Switch · Productive Workshop</p><h2 className="mt-2 text-2xl md:text-4xl font-semibold tracking-tight">{props.workshop.title}</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">{props.workshop.purpose}</p></div><div className="rounded-2xl border border-emerald-400/20 bg-emerald-400/5 px-4 py-3 text-right"><p className="text-[9px] uppercase tracking-[0.25em] text-emerald-300">File Folder</p><p className="mt-1 font-mono text-xs text-slate-200">{props.client.file_number}</p><p className="mt-1 text-[10px] text-slate-500">{props.folder?.status||'waiting'}</p></div></div></div>
    <div className="grid gap-0 lg:grid-cols-[260px_1fr]"><aside className="border-b lg:border-b-0 lg:border-r border-white/10 p-4 md:p-5"><p className="px-2 text-[9px] uppercase tracking-[0.25em] text-slate-500">Workshop functions</p><div className="mt-3 space-y-1.5">{modules.map(module=>{const Icon=icons[module]||Boxes;return <button key={module} onClick={()=>setActive(module)} className={`w-full flex items-center gap-3 rounded-xl px-3 py-3 text-left text-xs transition ${active===module?'bg-white/10 text-white':'text-slate-400 hover:bg-white/5 hover:text-white'}`}><Icon className="h-4 w-4"/><span>{module}</span><ChevronRight className="ml-auto h-3.5 w-3.5 opacity-40"/></button>})}</div></aside>
      <div className="min-w-0 p-5 md:p-8"><div className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-white/10 bg-black/30 p-5 md:col-span-2"><div className="flex items-center gap-3"><div className="rounded-xl bg-sky-400/10 p-2.5"><CircleDollarSign className="h-5 w-5 text-sky-300"/></div><div><p className="text-[9px] uppercase tracking-[0.22em] text-slate-500">Current formation</p><h3 className="mt-1 text-lg font-semibold">{active}</h3></div></div>
        {crypto&&active==='Market'?<div className="mt-6 grid gap-3 sm:grid-cols-3">{['BTC / USDT','ETH / USDT','SOL / USDT'].map(pair=><div key={pair} className="rounded-xl border border-white/10 bg-slate-900/70 p-4"><p className="text-xs font-semibold">{pair}</p><p className="mt-2 text-[10px] text-slate-500">Market connection prepared</p></div>)}</div>
        :active==='Business Store'?<div className="mt-6 space-y-4"><div className="rounded-xl border border-sky-400/20 bg-sky-400/5 p-5"><p className="text-[9px] uppercase tracking-[0.2em] text-sky-300">Public customer door</p><h4 className="mt-2 text-lg font-semibold">{props.businessStore?.name || props.client.business_name || props.client.name}</h4><p className="mt-2 text-xs text-slate-400">Customers can reach this store through the internet without creating a Weave account.</p>{publicUrl&&<div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>navigator.clipboard?.writeText(`${window.location.origin}${publicUrl}`)} className="inline-flex items-center gap-2 rounded-full border border-white/10 px-3 py-2 text-[10px] text-slate-300"><Copy className="h-3.5 w-3.5"/>Copy store link</button><a href={publicUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 rounded-full border border-white/10 px-3 py-2 text-[10px] text-slate-300"><ExternalLink className="h-3.5 w-3.5"/>Open store</a></div>}</div><div className="rounded-xl border border-white/10 p-5"><p className="text-[9px] uppercase tracking-[0.2em] text-slate-500">Customer activity</p><p className="mt-3 text-2xl font-semibold">{props.businessStore?.orders?.length||0}</p><p className="text-xs text-slate-500">recent requests / orders</p>{props.businessStore?.orders?.slice(0,3).map((order:any)=><div key={order.id} className="mt-3 rounded-lg bg-black/30 p-3 text-[10px] text-slate-400"><p>{order.customer_name} · {order.amount} {order.currency}</p><p className="mt-1">Payment: {order.payment_status || 'awaiting_payment'} · Request: {order.status}</p><p className="mt-1 truncate">Wallet: {order.customer_wallet || 'not supplied'}</p></div>)}</div></div>
        :active==='International Payments'?<div className="mt-6 space-y-4"><div className="rounded-xl border border-emerald-400/20 bg-emerald-400/5 p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-[9px] uppercase tracking-[0.2em] text-emerald-300">International payment door</p><h4 className="mt-2 text-lg font-semibold">Wise receiving profile</h4></div><span className={`rounded-full px-3 py-1 text-[9px] uppercase tracking-[0.15em] ${payments.enabled?'bg-emerald-400/10 text-emerald-300':'bg-white/5 text-slate-500'}`}>{payments.enabled?'Enabled':'Not enabled'}</span></div><p className="mt-3 text-xs leading-5 text-slate-400">Customers can be given the receiving details here and pay manually. Weave records the request; it does not log into Wise or execute a bank transfer.</p></div><div className="grid gap-3 md:grid-cols-2"><label className="text-xs text-slate-400">Account name<input value={payments.account_name||''} onChange={e=>setPayments({...payments,account_name:e.target.value})} className="mt-1 w-full rounded-lg border border-white/10 bg-black/30 p-3 text-sm text-white"/></label><label className="text-xs text-slate-400">Receiving email / identifier<input value={payments.receiving_identifier||''} onChange={e=>setPayments({...payments,receiving_identifier:e.target.value})} className="mt-1 w-full rounded-lg border border-white/10 bg-black/30 p-3 text-sm text-white"/></label><label className="text-xs text-slate-400">Payment link (optional)<input value={payments.payment_link||''} onChange={e=>setPayments({...payments,payment_link:e.target.value})} className="mt-1 w-full rounded-lg border border-white/10 bg-black/30 p-3 text-sm text-white"/></label><label className="text-xs text-slate-400">Supported currencies<input value={payments.supported_currencies||''} onChange={e=>setPayments({...payments,supported_currencies:e.target.value})} className="mt-1 w-full rounded-lg border border-white/10 bg-black/30 p-3 text-sm text-white"/></label><label className="text-xs text-slate-400">Service fee (%)<input type="number" min="15" max="30" step="0.5" value={payments.service_fee_percent} onChange={e=>setPayments({...payments,service_fee_percent:Number(e.target.value)})} className="mt-1 w-full rounded-lg border border-white/10 bg-black/30 p-3 text-sm text-white"/></label><label className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/30 p-3 text-xs text-slate-300"><input type="checkbox" checked={Boolean(payments.enabled)} onChange={e=>setPayments({...payments,enabled:e.target.checked})}/>Show international payment option on the public store</label></div><label className="block text-xs text-slate-400">Customer payment instructions<textarea value={payments.instructions||''} onChange={e=>setPayments({...payments,instructions:e.target.value})} rows={4} placeholder="Tell customers how to pay and what reference to send after payment." className="mt-1 w-full rounded-lg border border-white/10 bg-black/30 p-3 text-sm text-white"/></label><div className="flex items-center gap-3"><button onClick={savePayments} disabled={savingPayments} className="rounded-full bg-emerald-600 px-5 py-2.5 text-xs font-semibold disabled:opacity-50">{savingPayments?'Saving...':'Save payment door'}</button>{paymentMessage&&<span className="text-xs text-emerald-300">{paymentMessage}</span>}</div><p className="text-[10px] leading-5 text-slate-500">The 15%–30% service fee is a configurable business charge, not a guaranteed return. Keep Wise login credentials and bank security information out of this File Folder.</p></div>
        :<div className="mt-6 rounded-xl border border-dashed border-white/10 p-5 text-sm text-slate-400">{active} is a workshop function. Its operating system is formed here from the Client’s continuing Interaction in Motion.</div>}
        {crypto&&(active==='Buy'||active==='Sell')&&<div className="mt-4 rounded-xl border border-amber-400/20 bg-amber-400/5 p-4 text-xs text-amber-200">Execution is not represented as completed here. A real order requires an authorized exchange or wallet execution connection.</div>}
        </div>
        <div className="rounded-2xl border border-white/10 bg-black/30 p-5"><div className="flex items-center gap-2 text-slate-400"><Wallet className="h-4 w-4"/><span className="text-[9px] uppercase tracking-[0.22em]">Client Money Environment</span></div><div className="mt-4 space-y-3"><div className="rounded-xl border border-emerald-400/15 bg-emerald-400/5 p-3"><p className="text-[9px] uppercase tracking-[0.18em] text-emerald-300">Client Vault</p><p className="mt-1 text-xl font-semibold">{props.vault.balance.toLocaleString(undefined,{maximumFractionDigits:8})}</p><p className="text-[10px] text-slate-500">{props.vault.currency} · Administration can credit Flame Coin here.</p></div><div className="rounded-xl border border-violet-400/15 bg-violet-400/5 p-3"><p className="text-[9px] uppercase tracking-[0.18em] text-violet-300">Siblings Funds Wallet</p><p className="mt-1 text-lg font-semibold">{(props.vault.siblings_funds?.flameCoin||0).toLocaleString(undefined,{maximumFractionDigits:8})} <span className="text-[10px] text-slate-500">Flame Coin</span></p><p className="mt-1 text-[10px] text-slate-500">TRX {(props.vault.siblings_funds?.trx||0).toLocaleString()} · USDT {(props.vault.siblings_funds?.usdt||0).toLocaleString()}</p></div><div className="rounded-xl border border-sky-400/15 bg-sky-400/5 p-3"><p className="text-[9px] uppercase tracking-[0.18em] text-sky-300">Main Client Wallet</p><p className="mt-1 text-lg font-semibold">{(props.vault.main_wallet?.flameCoin||0).toLocaleString(undefined,{maximumFractionDigits:8})} <span className="text-[10px] text-slate-500">Flame Coin</span></p><p className="mt-1 text-[10px] text-slate-500">USDT {(props.vault.main_wallet?.usdt||0).toLocaleString()}</p></div></div><p className="mt-4 text-[10px] text-slate-500">Vault withdrawals require Administration approval. The three balances remain separate.</p>{props.vault.withdrawals?.[0]&&<p className="mt-2 text-[10px] text-slate-400">Latest Vault withdrawal: {props.vault.withdrawals[0].status}</p>}</div></div>
        <div className="mt-4 grid gap-4 md:grid-cols-3"><div className="rounded-2xl border border-white/10 bg-black/20 p-5"><div className="flex items-center gap-2"><GitBranch className="h-4 w-4 text-sky-300"/><p className="text-[9px] uppercase tracking-[0.22em] text-slate-500">Bridge</p></div><p className="mt-3 text-sm font-semibold">{props.bridge?.name||'Assigned Bridge'}</p><p className="mt-1 text-xs text-slate-500">Accompanies the Client and works with the company support structure.</p></div>
          <div className="rounded-2xl border border-sky-400/20 bg-sky-400/5 p-5"><div className="flex items-center gap-2"><Bot className="h-4 w-4 text-sky-300"/><p className="text-[9px] uppercase tracking-[0.22em] text-sky-300">Bridge AI</p></div><p className="mt-3 text-sm font-semibold">{props.bridgeAi.name}</p><p className="mt-1 text-xs leading-5 text-slate-400">{props.bridgeAi.purpose}</p><button onClick={report} disabled={reporting} className="mt-4 inline-flex items-center gap-2 rounded-full border border-sky-400/20 px-3 py-2 text-[10px] uppercase tracking-[0.15em] text-sky-200 disabled:opacity-50"><RefreshCw className={`h-3.5 w-3.5 ${reporting?'animate-spin':''}`}/>{reporting?'Reporting…':'Report to Admin'}</button>{props.bridgeAi.last_report&&<p className="mt-3 text-[10px] leading-4 text-slate-500">Last report recorded: {props.bridgeAi.last_report.summary}</p>}{reportMessage&&<p className="mt-3 text-[10px] text-emerald-300">{reportMessage}</p>}</div>
          <div className="rounded-2xl border border-white/10 bg-black/20 p-5"><div className="flex items-center gap-2"><Users className="h-4 w-4 text-violet-300"/><p className="text-[9px] uppercase tracking-[0.22em] text-slate-500">Company Support · Approved Agents</p></div><div className="mt-3 space-y-2">{props.approvedAgents.slice(0,4).map(agent=><div key={agent.id} className="flex items-center gap-2 text-xs text-slate-300"><ShieldCheck className="h-3.5 w-3.5 text-emerald-300"/>{agent.name}</div>)}{props.approvedAgents.length===0&&<p className="text-xs text-slate-500">Administration has not approved company support for this workshop yet.</p>}</div></div></div>
        <div className="mt-4 rounded-2xl border border-white/10 bg-black/20 p-5"><div className="flex items-center gap-3"><Boxes className="h-5 w-5 text-violet-300"/><div><p className="text-[9px] uppercase tracking-[0.22em] text-slate-500">Formation layer</p><h3 className="mt-1 text-sm font-semibold">Business + Advanced Technology</h3></div></div><p className="mt-3 max-w-4xl text-xs leading-6 text-slate-500">The File Folder is the Client’s operating environment. The Business Store opens a public customer door while the private workshop remains under the Client’s authenticated control.</p></div>
      </div></div>
  </section>
}
